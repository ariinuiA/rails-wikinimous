<div class="container">
  <% @articles.each do |article| %>
    <div class="d-flex flex-column mb-5 align-items-center">
      <h1><%= link_to article.title, article_path(article.id), class:"text-decoration-none" %></h1>
      <p><%= article.content %></p>
    </div>
  <% end %>
</div>
